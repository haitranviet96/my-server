services:
  caddy:
    build: ./caddy
    restart: unless-stopped
    env_file:
      - ./caddy/caddy.env
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile
      - ./caddy/site:/srv
      - caddy_data:/data
      - caddy_config:/home/haitv/my-server/caddy

volumes:
  caddy_data:
  caddy_config:
