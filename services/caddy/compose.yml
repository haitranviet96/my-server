services:
  caddy:
    build: ./caddy
    restart: unless-stopped
    env_file:
      - config.env
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
    extra_hosts:
    - "host.docker.internal:host-gateway"

volumes:
  caddy_data:
  caddy_config:
